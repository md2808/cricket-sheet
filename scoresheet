 #include"declaration.h"
 #include "welcome.h"
 #include "menu.h"
 #include "newscoresheet.h"
 int main()
 {
 int a,key;
 charb;
 file*fnew;
 char lastinput;
 while(true)
 {
  welcome();
  system("cls");
  menu();
  ch=getch();
  char ch; 
  switch(ch)
{
    case'1'; 
    fileopen(ch);

    case '2';
  fileopen(ch); 
  getch();

   case '3';
  system("cls");
  exit(0);
    
}
}

}
